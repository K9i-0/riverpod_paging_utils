appId: ${APP_ID}
name: Launch Test - アプリ起動確認
---
# アプリを起動（状態クリア）
- launchApp:
    clearState: true

# Paging Demo が表示されることを確認
- extendedWaitUntil:
    visible: "Paging Demo"
    timeout: 5000

# リストアイテムが表示されることを確認（最初のアイテム）
- assertVisible:
    id: "sample-item-0"
